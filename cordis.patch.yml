# dsh-topics-memory bundle patch: mounts the plugin into the profile's
# assembly tree under the stable plugin id `dsh-topics-memory`. It registers
# the `topics` settings namespace, the topic tools, the /topics slash command,
# and the session-event observer (injection + two-stage observation).

- insert:
    - id: dsh-topics-memory
      name: '@aiwayds/dsh-topics-memory'
